var animator = {
	var L_HAND_X = 0;
	var R_HAND_X = 0;

	animateSwap:function(swap) {
		var 
	},

	getThrowEndX:function(theThrow, throwIndex) {
		var endX = (theThrow % 2 == 0) ? SAME_HAND_X : OTHER_HAND_X;
		return endX;
	},

	getBallPosition:function(theThrow, tickFrame) {

	}
}